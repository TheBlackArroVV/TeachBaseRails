<h1>Editing Route</h1>
0
<%= render 'form', route: @route, hidden: false %>

<%= link_to 'Show', admin_route_path(@route), class: 'btn btn-primary' %> |
<%= link_to 'Back', admin_routes_path, class: 'btn btn-secondary' %>
