<h1>Editing Train</h1>

<%= render 'form', train: @train %>

<%= link_to 'Show', admin_train_path(@train), class: 'btn btn-primary' %> |
<%= link_to 'Back', admin_trains_path, class: 'btn btn-secondary' %>
