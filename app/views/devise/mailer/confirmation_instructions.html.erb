<p><%= t('devise.welcome') %>, <%= @email %>!</p>

<p><%= t('devise.mailer.confirm_here') %>:</p>

<p><%= link_to t('devise.mailer.confirm'), confirmation_url(@resource, confirmation_token: @token) %></p>
